# Python dependencies for the site renderer (scripts/render-hub.py) and the
# Field Note social-card generator (scripts/build-og-cards.py).
# Pinned for local/CI parity — bump deliberately so generated output can't
# drift. A pinned Pillow is byte-stable run-to-run on a given OS, but its
# bundled FreeType rasterizes a few bytes differently across platforms
# (ubuntu vs Windows), so the rebuild-hub workflow (ubuntu) is the source of
# truth for assets/og — don't commit cards regenerated on a non-Linux box.
markdown==3.10.2
Pillow==10.4.0
numpy==2.3.4
# Read by tests/test_visual_diff.py, which PARSES .github/workflows/*.yml to
# pin two wirings that must never exist (never on push, never inside
# rotate-theme.yml). It was imported without being declared, and every
# workflow installs exactly `-r requirements.txt pytest` — so PR CI went red
# with ModuleNotFoundError while the same suite passed locally, and the
# unattended rotation would have aborted on every theme from 2026-09 on.
# A real YAML parse is the right test; the defect was not declaring it.
PyYAML==6.0.2
