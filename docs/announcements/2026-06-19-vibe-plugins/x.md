# vibe-plugins (the thirteen) — X / Twitter copy

**Angle:** evergreen suite. Lead with the count and the arc — thirteen
plugins, one shared spine, the full lifecycle from idea to maintained. The
spine is the thesis: an agent that reads your code before it touches it. No
version numbers (they age the post) — counts and capabilities only. Plain
text (X strips markdown). Sparing emoji per the launch emoji caveat — one
light spark, not a sprinkle. Em-dashes welcome.

**Char counts are X-weighted:** every URL bills at 23 chars; `→`, `✦`, and
`✨` cost 2 each. Limit 280 for a single post.

---

## Standalone post (280 chars, X-weighted — fits the 280 limit exactly)

```
Thirteen Claude Code plugins. One arc: idea → shipped → maintained. ✦

An agent that reads your code before it touches it — the whole lifecycle around the code, where vibe-coded apps rot.

/plugin marketplace add estevanhernandez-stack-ed/vibe-plugins

Imagine Something Else.
```

## Thread (each tweet < 280, X-weighted)

```
1/ ✦ Thirteen Claude Code plugins. One shared spine: an agent that reads your code before it touches it. They cover the whole lifecycle around the code — scoping, docs, tests, security, prompts, maintenance — exactly where vibe-coded apps quietly rot. (251)

2/ Build → Vibe Cartographer takes an idea to a running app through a build chain you can actually steer: onboard, scope, PRD, spec, checklist, build. Keystone writes the CLAUDE.md it all rests on first. Doc writes the docs nobody volunteers for. (244)

3/ Harden → Vibe Sec audits twelve security concerns, writes the fix, drops a CI gate so the hole stays closed. Vibe Test catches the harness cheerfully reporting 0% coverage. Vibe Prompt turns the same lens on the prompts your app ships — a behavioral eval against your model. (276)

4/ Maintain → Vibe Iterate decides what to build next. Vibe Walk builds the onboarding tour users finish, and tells you when one won't help. Vibe Wrap closes the session from the trail the rest already left. The arc, end to end. (226)

5/ Thirteen plugins, one spine, the whole arc. Install the family:

/plugin marketplace add estevanhernandez-stack-ed/vibe-plugins

Read the Field Note → https://626labs.dev/editorial/2026-06-19-vibe-plugins-thirteen/

Imagine Something Else. (200, URL counts as 23)
```
