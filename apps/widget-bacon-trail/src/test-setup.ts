// Vitest setup — loaded via vite.config.ts `test.setupFiles`.
// @testing-library/jest-dom extends `expect` with DOM-aware matchers like
// `.toBeInTheDocument()`, `.toHaveClass()`, etc.
import '@testing-library/jest-dom/vitest';
