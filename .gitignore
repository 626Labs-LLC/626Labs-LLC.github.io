# Tool artifacts
.qodo/
.playwright-mcp/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/

# Python build leftovers (from scripts/)
__pycache__/
*.pyc
.venv/

# Large source assets — processed thumbnails live in repo, sources do not
assets/level_3 Este.gif

# Admin dashboard — online at 626labs.dev/admin-dashboard.html but gated.
# Anyone can load the URL; the lockscreen blocks access until a valid
# fine-grained GitHub PAT (Contents: Write on this repo) is pasted.
# PAT lives only in the visitor's localStorage. robots: noindex,nofollow.

# Local plugin / agent state — regenerated locally, never tracked
.vibe-iterate/
.vibe-doc/
.claude/626labs-context.md

# Generated docs (vibe-doc output) + session wraps (vibe-wrap output) —
# regenerate on demand; the wraps also name private sibling repos.
docs/generated/
docs/session-wraps/

# Personal documents — this repo is PUBLIC; keep these local-only.
Estevan-626-Resume.pdf
Profile.pdf
anthropic_fellows.*
docs/github-sponsors-profile.md

# Stray brand files NOT produced by export-brand.py (its output set is the
# icon + banners; these portrait crops are unreferenced churn). Ignored so
# they can't be committed to the generated assets/brand/ dir by accident.
assets/brand/logo-portrait-128.png
assets/brand/logo-portrait-256.png
.gitnexus
